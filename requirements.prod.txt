# Production-only dependencies for Docker image.
# torch/torchvision excluded — classifier runs on SageMaker (CLASSIFIER_BACKEND=sagemaker).
# Training, testing, and frontend packages excluded.
# Versions pinned to avoid pip resolver backtracking (ResolutionTooDeep).

# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic>=2.10.0
python-multipart==0.0.9

# AWS SDK
boto3>=1.37.0,<2.0
botocore>=1.37.0,<2.0

# LangChain ecosystem — pinned to a known-compatible set
langchain-core==0.3.29
langchain==0.3.14
langchain-aws==0.2.14
langchain-community==0.3.14
langsmith==0.2.11

# LangGraph
langgraph==0.2.73

# Image handling (for SageMaker payload prep)
Pillow>=11.0.0
numpy>=1.26.0

# Vector Search
faiss-cpu>=1.12.0
rank_bm25==0.2.2

# PDF Parsing
pymupdf==1.24.11
pdfplumber==0.11.4

# Language Detection
lingua-language-detector==2.1.1

# HTTP
httpx==0.27.0

# Logging
structlog==24.4.0
watchtower==3.2.0

# Config
python-dotenv==1.0.1
