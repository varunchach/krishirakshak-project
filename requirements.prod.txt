# Production-only dependencies for Docker image.
# torch/torchvision excluded — classifier runs on SageMaker (CLASSIFIER_BACKEND=sagemaker).
# Training, testing, and frontend packages excluded.

# Core API
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic==2.9.0
python-multipart==0.0.9

# AWS SDK
boto3>=1.35.0
botocore>=1.35.0

# LangGraph / LangChain
langgraph>=0.2.0
langchain-aws>=0.2.0
langchain-core>=0.3.0
langchain-community>=0.3.0

# Image handling (for SageMaker payload prep)
Pillow>=10.4.0
numpy>=1.26.0

# Vector Search
faiss-cpu>=1.8.0
rank_bm25>=0.2.2

# PDF Parsing
pymupdf>=1.24.0

# Language Detection
lingua-language-detector>=2.0.0

# HTTP
httpx>=0.27.0

# Logging
structlog==24.4.0
watchtower==3.2.0

# Config
python-dotenv==1.0.1
